# AI Voice Inputer Python 依赖
# Ubuntu 24.04 AI 语音输入法

# 核心依赖
faster-whisper>=1.0.0      # Whisper 语音识别 (CTranslate2 优化版)
sounddevice>=0.4.6         # 音频录制
numpy>=1.24.0              # 数组处理
pynput>=1.7.6              # 全局热键监听

# 配置管理
PyYAML>=6.0                # YAML 配置解析

# LLM 增强 (可选)
requests>=2.31.0           # HTTP 请求 (调用 LLM API)

# 开发依赖 (可选)
# pytest>=7.0.0            # 测试框架
# black>=23.0.0            # 代码格式化
# mypy>=1.0.0              # 类型检查
